interface IFormStateNames {
  tower: string,
  floor: string,
  room: string,
  date: string,
  comment: string,
}

const formStateNames: IFormStateNames = {
  tower: 'tower',
  floor: 'floor',
  room: 'room',
  date: 'date',
  comment: 'comment',
}

export default formStateNames